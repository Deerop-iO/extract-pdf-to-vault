# pdf-to-vault environment template. Copy to .env and fill in if needed.
# Phase 1 (PyMuPDF + pymupdf4llm) needs NO secrets or network access.
# Reserved for optional Phase 2 features (e.g. an LLM enrichment provider).

# Example (Phase 2, enriched tier only):
# OPENAI_API_KEY=
